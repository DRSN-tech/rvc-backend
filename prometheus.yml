global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "app"
    static_configs:
      - targets: ["app:2112"]  # имя сервиса из docker-compose
    metrics_path: "/metrics"